import 'package:flutter/material.dart';

class LoginPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Login Page (to be completed later)', style: TextStyle(fontSize: 20)),
      ),
    );
  }
}
